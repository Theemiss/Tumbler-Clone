<%= form_for ([@post, @post.comments.build]) do |f| %>
<div>
<%= f.label :name %><br>
<%= f.text_field :name %>
</div>
<div>
<%= f.label :body %><br>
<%= f.text_area :body%>
</div>
<br>
<%= f.submit 'comment' %>


<% end %>