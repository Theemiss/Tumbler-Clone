
<% @posts.each do |post| %>
<div class="container">
    <h2>
        <%= link_to post.title , post %>
    </h2>
    <p> <%= time_ago_in_words(post.created_at) %></p>
    <p> <%= truncate(post.body,length:100) %></p>
</div>
<% end %>